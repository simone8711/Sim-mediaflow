name: Mediaflow Keep Alive
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep Alive
        run: |
          curl -I https://sim-mediaflow-xyz.onrender.com/health
